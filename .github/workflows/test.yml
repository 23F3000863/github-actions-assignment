name: IITM Assignment

on:
workflow_dispatch:

jobs:
test:
runs-on: ubuntu-latest

```
steps:
  - name: Checkout
    uses: actions/checkout@v4

  - name: 23f3000863@ds.study.iitm.ac.in
    run: echo "Hello IITM"

  - name: Finish
    run: echo "Workflow completed"
```
